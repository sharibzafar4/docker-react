sudo: required
services:
    -docker

before-install:
    -docker build -t sharibzafar4/docker-react-image -f Dockerfile.dev .

script:
    docker run sharibzafar4/docker-react-image npm run test -- --coverage